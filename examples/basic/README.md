# Basic example

From the repository root:

~~~bash
npm install
npm run build
npx serve .
~~~

Then open `examples/basic/index.html`.

The example authors every destination in the primary list and starts the More control hidden. The plugin moves original list-item nodes only after JavaScript initializes and the rendered navigation no longer fits on one line.
